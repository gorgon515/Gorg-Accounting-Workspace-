{
  "name": "aria-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "ARIA — an integrated, voice-driven desktop assistant (stocks, accounting, study, productivity). Vertical slice: Stocks.",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --enable-logging"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
