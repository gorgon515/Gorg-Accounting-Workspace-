# HELIOS Intelligence Sidecar — runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
requests>=2.31
# Document intelligence — real text extraction (digital documents)
pypdf>=4.0
openpyxl>=3.1
python-docx>=1.1
# Optional: scanned-image OCR (also needs the system `tesseract` binary)
# pytesseract>=0.3.10
